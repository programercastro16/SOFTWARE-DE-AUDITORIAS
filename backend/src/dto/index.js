const UserDTO = require('./UserDTO');
const AuditDTO = require('./AuditDTO');
const EvidenceDTO = require('./EvidenceDTO');
const AuthDTO = require('./AuthDTO');
const AuditItemDTO = require('./AuditItemDTO');

module.exports = {
  UserDTO,
  AuditDTO,
  EvidenceDTO,
  AuthDTO,
  AuditItemDTO
};
